<h1>状态徽章</h1>
<p>SITE_NAME 提供状态徽章，您可以嵌入到 README、内部
仪表板或公共状态页面中。每个 SITE_NAME 徽章报告项目中所有检查项的
组合状态、标记了特定标签的检查项的状态，
或单个特定检查项的状态。</p>
<p><img alt="&quot;Badges&quot;页面" src="IMG_URL/badges.png" /></p>
<p>徽章具有公开但难以猜测的 URL。徽章不会透露除
徽章标签及其对应检查项的聚合状态之外的信息。
无法从徽章 URL 反向工程出 ping URL。</p>
<h2>徽章状态</h2>
<p>每个徽章可以处于以下三种状态之一：</p>
<ul>
<li><strong>up</strong>（绿色）——所有匹配的检查项均正常。</li>
<li><strong>late</strong>（橙色）——至少有一个检查项运行延迟（但尚未超过其宽限期）。</li>
<li><strong>down</strong>（红色）——至少有一个检查项当前宕机。</li>
</ul>
<p>默认情况下，SITE_NAME 显示仅报告 <strong>up</strong> 和 <strong>down</strong> 状态（并将 <strong>late</strong> 视为 <strong>up</strong>）的徽章 URL。使用"Badge states"
单选按钮，您可以切换到报告所有三种状态的替代 URL。</p>
<h2>徽章格式</h2>
<p>SITE_NAME 提供三种不同格式的徽章：</p>
<ul>
<li>SVG：返回一个 SVG 文档，您可以直接在 <code>&lt;img&gt;</code> 元素或
  Markdown 文档中使用。</li>
<li>JSON：返回徽章标签和当前状态作为 JSON 文档。如果您想自己渲染徽章，
  请使用此格式。这也可以作为与托管状态页面的集成
  点：指示您的状态页面提供商监控徽章 URL 并在返回的数据中查找关键字"up"。</li>
<li>Shields.io：返回徽章标签和当前状态作为
  Shields.io 兼容的 JSON 文档。请参阅 <a href="https://shields.io/endpoint">Shields.io 文档</a>
  了解如何使用。使用 Shields.io 生成徽章的主要好处是
  Shields.io 支持的额外视觉样式和自定义选项。</li>
</ul>